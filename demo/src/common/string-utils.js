export const REQUEST = (actionName) => {
  return actionName + '_REQUEST';
};

export const SUCCESS = (actionName) => {
  return actionName + '_SUCCESS';
};

export const FAILURE = (actionName) => {
  return actionName + '_FAILURE';
};
